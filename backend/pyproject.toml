[project]
name = "rpa-btime-criptomoedas"
version = "0.1.0"
description = "Coleta comparativa de criptomoedas por web scraping e API pública."
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4==4.13.5",
    "fastapi==0.141.1",
    "pydantic==2.13.5",
    "requests==2.32.5",
    "uvicorn==0.53.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

